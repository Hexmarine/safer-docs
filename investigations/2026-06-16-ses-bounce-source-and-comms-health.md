# SES bounce source + comms health (2026-06-16 → 06-18)

> Archived 2026-07-03 from the Claude memory file `ses-sns-health-and-bounce-source.md` during a memory compaction.
> Point-in-time record — file:line references and live-state claims are as of the dates in the body.


Verified 2026-06-16 (AWS acct 747293622182, ap-southeast-2) that email/SMS comms work, and traced an SES bounce concern.

## Health (read-only AWS)
- **SES** ✅ — `EnforcementStatus HEALTHY`, `SendingEnabled`, production access, domain `sensorglobal.com` DKIM-verified. Actively sending. Backend *defaults* to SendGrid (`EMAIL_PROVIDER`) but prod routes live mail via **SES SMTP** (nodemailer transport in `MailManagerClass.sendMailViaSesWithHeaderFooter`). No SES configuration set / event destination exists.
- **SNS (SMS)** ✅ infra — out of sandbox, `MonthlySpendLimit $700`, sender ID `SENSOR`, delivery logs → S3 `smokesnslogs`. **Confirmed end-to-end**: direct `boto3 sns.publish` to a real handset delivered. SMS was *dormant* (≈$0 June spend, flatlined late May) purely because the app-side `SEND_SMS=='1'` gate is off — not a fault. See [[sensor-outgoing-comms-map]].

## Env gotcha — AWS CLI broken for some ops
`aws-cli/2.31.35` on `Python/3.14.4` throws `badly formed help string` building certain subcommand parsers (e.g. `sns publish`) — argparse `%`-format check tightened in 3.14. Read-only calls work; mutating/complex ones fail. Same root cause as the broken SSM `send-command` (see [[odoo-prod-db-access]]). **Workaround: use boto3 directly** (installed: boto3 1.43.11). The CLI version-bump is the real fix.

## ⭐ CORRECTED ROOT CAUSE (2026-06-17, SES capture = ground truth)
The daily batch's dominant component is **OVERDUE-JOB reminder emails**, NOT invitation-reminders (the invitation-reminder identification below was a WAF/static-analysis hypothesis, REFUTED by SES capture: overdue-job reminders = 542 of captured sends + 49 of 50 bounces). Cron `checkOverdueJobsCron` → `checkDuejobsAndNotify` (jobs.controller.ts:254 / jobs.entity.ts:8292,8351); weekly per-contractor at contractor-local **Monday 08:00**, **one email per overdue job**, recipient = the assigned **contractor** (`tradePersonId` → tbl_admins.email).
- **Overdue = `jobTime`(epoch-ms) < now AND status NOT IN (0 INACTIVE,3 REJECTED,5 COMPLETED,9 CLOSED).** Backlog quantified 2026-06-17: **1,475 overdue jobs / 25 contractors / 27 agencies.** Status mix: 1,411 ACCEPTED (accepted, never completed), 50 PENDING, 14 NOE_SCHEDULED. Age: 644 @31-90d, 338 @91-180d, 298 @181-365d, 25 @>365d, 170 @0-30d. Top agencies: Emma Beauman(46607)=372, Chloe Carlin(27284)=224, Haven Home Safe(37413)=222, Sandra Mathieson(13191)=186, Tammy Smith(34504)=182, Trent Lloyd(11769)=106.
- **The 8% bounce = 2 contractor recipients (107 overdue jobs → ~107 bounces/wk):** John Trotter `sensor@homesafetyballarat.com` (id 37383) = 80 jobs; `@sensorglobal.com` test/internal = 27 (Sensor id10380=15, Support Service Provider id30465=5, CSIRO id29498=4, +3 test). All contractor_status=1 (active).

## ⭐⭐ FURTHER-CORRECTED ROOT CAUSE (2026-06-18) — it's the SES account SUPPRESSION LIST, not dead addresses
The 06-17 "2 dead/placeholder" framing was half-wrong. **Every captured bounce is `Permanent / OnAccountSuppressionList`** — SES is NOT attempting delivery and being rejected; it **refuses to send** because the address is on the **account-level suppression list**, and still counts each attempt as a fresh bounce (self-perpetuating loop that keeps the rate elevated as long as the app re-sends). Falsified premises: `homesafetyballarat.com` is a **LIVE Google Workspace domain** (valid MX/NS), NOT defunct; John Trotter (37383) is an **ACTIVE** contractor (639 completed + 125 closed jobs, assigned work to 2026-06-11). `sensor@homesafetyballarat.com` was auto-suppressed **2026-05-18 reason=BOUNCE** and re-bounces daily since. **Real fix has 3 parts:** (1) stop the *sends* to suppressed addresses; (2) for valid mailboxes, remove from suppression (`sesv2 delete-suppressed-destination`, Tier 2) once the address is corrected; (3) **structural** — `MailManager` does NOT check the suppression list before emailing, so any hard-bounced address re-bounces forever (code fix, Category B/C). 
- **APPLIED 2026-06-18** (`applied-changes.md`): closed the **27 internal/test `@sensorglobal.com` overdue jobs** (`tbl_jobs` status→9; ids incl. test agencies "Manufacturer Agency Test Super User"/internal health-checks/test tradies) so they stop re-emailing suppressed test addresses. ROW_COUNT=27 verified.
- **STILL OPEN:** John Trotter's **80 jobs are real** (Goldfish Real Estate et al.) — needs his correct working email (from the agencies) + suppression-entry removal, NOT closing real jobs or blanking an active contractor's email; the structural sender-suppression check; broader 1,475 ACCEPTED backlog triage. Extracts: `ops-and-extracts/overdue-bounce-clusters-2026-06-17.csv` (107) + `overdue-jobs-triage-2026-06-17.csv` (1,475) + `ses-capture-bounces-2026-06-17.csv` (all PII, uncommitted).

## ⭐ Import-warning email sources (2026-06-17 full SES capture, 1,391 events)
Full daily capture (subjects, ground truth): 695 Send / 645 Delivery / 50 Bounce. Composition is **day-of-week-dependent** — mid-week (Wed) is swap-transactional, NOT overdue-reminders (those are the Mon spike). Top: job-assigned-to-contractor 82, **data-import warnings 68**, job-arranged-to-resident 32, install-complete 20, + per-property alert/test tail. Senders: donotreply@sensorglobal.com (1,382). The 68 import-warning emails = 2 broken feeds, BOTH chronic, neither a real property removal — they're the live face of the [[haven-mass-deactivation-2026-06]] full-snapshot-reconcile risk:
- **S114 "Sensor Live Property not included in your data import"** (template d-2c8a…, jobs.entity.ts `sendMissingPropertyEmail`): fires per ACTIVE property whose `GUID` is absent from an agency's latest feed → writes `tbl_property_review` (status=0 PENDING, isCurrentImport=1) + emails the agent/agency. **Live case: Victoria Munro (agency 44572), PropertyTree.** Her feed returns only **2 of 20** active props (tbl_property_files type=propertytree status=1 totalProperty=2, daily 19:00 UTC) → **same 18 active props re-flagged EVERY day** since ≥06-12 → 18 S114 emails/day, 18 props perpetually "in review". Broken/partial PropertyTree scope (limited API key/portfolio), not removals. **Lever: `tbl_import_source` id167 (agencyId 44572, source=propertytree) status 1→0** stops the broken pull + the daily noise + the deactivation exposure (Tier-1, reversible) — but freezes her sync; better is fixing the PropertyTree scope. At-risk 18 props extracted → `ops-and-extracts/import-warning-atrisk-2026-06-17.csv`.
- **"Data Import Error – {agencyBusinessName}"** (PROPERTY_SYNC_FAIL): whole-feed failure. **Live case: Jens Veal Byrne Pty Ltd (agency 37567 = Chris Byrne), ConsoleCloud — a ZOMBIE integration.** Agency is **DEACTIVATED** (tbl_admins.status=0, email renamed `chris.DEACTIVATED@…`) yet its ConsoleCloud sync still runs **~16×/day, all failing** (tbl_property_files type=consolecloud status=2 totalProperty=0), each firing a Data Import Error email, for ~11 months. ConsoleCloud token `tbl_console_cloud_api_tokens` id5 = `authorized=0` (OAuth never completed/revoked), has_refresh=1, officeId 63896, untouched since 2025-07-23. **`tbl_import_source` id153 is ALREADY status=0 — proves ConsoleCloud is NOT gated by tbl_import_source** (it's webhook/token-driven; only propertyme/propertytree honour the cron `required:true` status gate). **Lever: delete/deauth token id5** (or remove the CC webhook) to stop the retries+emails. ConsoleCloud trigger = WEBHOOK push (`/console-cloud-hook` → getDataFromConsoleCloud → getConsoleCloudConfiguration maps officeId→agency; no null-guard on `.consoleCloudOfficeId`), so import_source.status / authorized flags don't stop it — deleting the token row makes the webhook null-deref BEFORE the email-send path.
- **APPLIED 2026-06-17** (writable primary, `applied-changes.md`): (1) `tbl_import_source` id167 (44572 propertytree) status 1→0 = Munro PropertyTree PAUSED (legit agency — feed scope still needs fixing before re-enable; rollback id167→1). (2) `tbl_console_cloud_api_tokens` id5 DELETED = 37567 zombie killed (office 63896 now 0 mapping rows; side-effect: webhook now logs TypeError instead of emailing — Console Group must deregister office to fully silence; rollback = re-auth recreates it, held no live value). **Audit conclusion:** only these 2 agencies were firing the 68 import emails/day; all other PropertyTree agencies legit/quiet.
- **APPLIED 2026-06-18** (`applied-changes.md`): deleted CC tokens **id3 (agency 16714/office 80886) + id4 (16715/80887)** — authorized creds on agencies absent from tbl_admins; **dormant** (last consolecloud import 2025-06-30 / 2026-01-12), so hygiene not noise. ROW_COUNT=2. **Left deliberately:** 86 orphaned enabled `tbl_import_source` rows (72 csv / 7 propertyme / 5 propertytree / 2 consolecloud) — all inert (csv=no cron; propertyme/propertytree crons inner-join active agencies that are gone; consolecloud is token-driven), deleting them is cosmetic.
- **PropertyMe dormant-since-04-09 — ROOT-CAUSED 2026-06-18:** all 18 agencies stopped the SAME day (PropertyTree/ConsoleCloud kept running) ⇒ shared cause. Cron still fires (agency-05:00 gate) but `getPropertyMeToken`→`getTokenByRefreshToken` (shared `PROPERTYME_API_CLIENT_ID/SECRET` → `PROPERTYME_API_TOKEN_URL`) has failed for every agency since 04-09; errors **double-swallowed** (returns undefined → cron `continue`s → no `tbl_property_files` row, no alert; token table newest updatedAt also 04-09). No code deploy to properties.entity.ts in-window ⇒ external: shared PropertyMe app credential expired/rotated (or refresh-token chain broke). Fix = verify/refresh credential in Secrets Manager (Tier 2) + possibly per-agency OAuth re-auth (product/ops). NOT a DB mutation.
- **Stale INVITED (1,289 props status=5) RE-SCOPED 2026-06-18 — NOT a noise source:** `invitationReminder()` only fires at 1/2/4/5 weeks post-invite then stops; the 1,252 props >6mo old send ZERO email (SES capture = 0 invite bounces). So no mutation done/warranted — a 1,289-row bulk change is zero-benefit + Category-C mass-mutation risk + a product decision. See docs/gaps.md item 8.

## The bounce trace — EARLIER HYPOTHESIS (invitation-reminder; superseded, kept for trail)
- "687 sent/24h" is **account-wide SES** (shared by sensor backend, SSO, safer-ops, sandbox/UAT, likely Odoo) — not one service.
- **Hourly CloudWatch AWS/SES breakdown** revealed it was ONE batch: **597 sends + 50 bounces at 07:57 UTC** (17:57 Melbourne); rest of day ~90 sends / 6 bounces. So one job owns ~8.4% bounce.
- **The batch is DAILY at ~07:30–08:00 UTC (≈17:30 Melbourne), started 2026-06-10** — the Haven mass-deactivation RECOVERY date ([[haven-mass-deactivation-2026-06]]), NOT the swap start (06-12). It resumed full volume when the recovery restored the fleet to ACTIVE. Daily volume 478→640; bounces climbing 29→56. **Trigger = recovery, not SIM activity.**
- **IDENTIFIED (WAF logs `aws-waf-logs-prod`):** scheduler = dedicated EC2 box **`i-0392d1fca678f1a21` = smoke-prod-api-cron-server** (t2.micro, EIP 54.153.177.72, priv 10.0.2.167) — the "crontab not in repo". The ~600/day batch (daily ~07:54 UTC = 17:54 Melbourne; from full 48h hourly profile) = the **INVITATION-REMINDER cron family**: WAF over the *real* batch window (07:50–08:05 UTC) shows 3 endpoints firing ONCE: `/users/properties/invitation-reminder`, `/users/users/invite-reminder`, `/users/properties/invitation-reminder-to-providerPays-agencies`. `invitationReminder` (properties.entity.ts:13344) reminds properties `status=INVITED(5)` invited 1/2/4/5 wks ago → landlord/tenant (S000-I invite template). **1,289 properties stuck INVITED** (agency 23740 kellie Gustowski=747, Catherine Taylor 179, Tennille Mugridge 99…; HAVEN 37413 = 0 INVITED → batch is NOT Haven, NOT the swaps). Bounces = stale invite addrs. (Property-level invitation-reminder matched 0 by exact invitedAt today ⇒ bulk is sibling invite-reminder/providerPays.) RULED OUT: syncPropertyMe/TreeCron (gate=agency-local 05:00=19:00 UTC, Haven is csv-sourced), notifyTenantCron, disconnect cron, summary/queue crons.
- **CONFIG GATES to stop the reminder emails:** (1) NO global SEND_EMAIL flag. (2) Reminder only targets `status=INVITED('5')` → resolve/cancel the 1,289 stuck invitations (root cause; esp. agency 23740's 747). (3) Built-in cap: reminders fire only at 1/2/4/5 wks post-invite then stop. (4) Surgical lever = suspend the 3 reminder cron lines on smoke-prod-api-cron-server's crontab (stops just these; needs host/SSM). Stale-address cleanup still needs SES event capture (runbook 15).
- **⚠️ SEPARATE GLEAN — `syncPropertyMeCron` daily UNGUARDED full-snapshot reconcile.** (Different cron, fires AU-local 05:00=19:00 UTC, ~19 PropertyMe agencies ~926 active props; NOT the email batch, NOT Haven.) `syncPropertyMeCron → processUploadProperties → jobsEntity.addPropertyInReviewOnCSVImport` (properties.entity.ts:14342) = the SAME full-snapshot reconcile behind the 2026-06-04 mass deactivation [[haven-mass-deactivation-2026-06]]. Daily, automated; any ACTIVE prop absent from that day's feed → 48h review-sweep deactivation. Runbook-14 manual-CSV controls DON'T cover it. One bad/partial feed (token expiry) re-triggers mass deactivation. **Standing prod risk.** CONFIG GATE: `tbl_import_source.status=0` per agency (source=propertyme/propertytree) disables that agency's auto-sync entirely (cron uses required:true inner join). `tbl_admins.autoDeactivation` does NOT prevent deactivation, only ON=immediate vs OFF=48h-review timing.
- Property `status` enum (PROPERTY_STATUS, app.ts:1256): 0=INACTIVE 1=ACTIVE 2=DELETED 4=NEW 5=INVITED 8=ACCEPTED 10=DISCONNECTED 11=DEACTIVATED. "Active"=`status='1'`=live monitored property. Haven 37413: 908 active, 220 accepted, 71 new, 2 disconnected.

## Hub connectivity check (2026-06-16) — reassuring
Tenants ARE on live hubs. 2,440/2,482 active-prop hubs show `connectedStatus='0'` BUT 2,439 heartbeat within 2 days (only 1 truly stale) — `connectedStatus`/`disconnectionEmailSent` are STICKY flags, real liveness = `lastConnectionTestDate` daily heartbeat (see [[sensor-device-state-schema-gotchas]]). The cron's `connectedStatus IN ('1','0')` filter is a no-op. So the bounce issue is **stale email addresses, NOT dead hubs**. (Haven 37413 = 867 of the '0' hubs; expected during swap, heartbeating fine.)

## Gating analysis
NO global email kill-switch (no `SEND_EMAIL` flag; only `SEND_SMS`). The batch isn't one gateable cron → no clean toggle. Levers (all imperfect): suspend the external cron at PM2/EC2 crontab (blunt, crontab not in repo, exact job unpinned); per-property `alarmTestNotificationSettingId`/per-user `notificationSetting` (path-specific); code-level feature flag (class-C change). **Gating is the wrong tool — it'd suppress good mail too. The fix is bounce CAPTURE + address hygiene** (SES suppression list already auto-drops repeat bouncers).

## Observability gap (highest-value fix)
The batch is persisted **nowhere**: Mongo `tbl_logs` logs only ~87/day business emails (job lifecycle); `tbl_communications_queue` (MySQL) dormant since 2026-03-26; `tbl_emails`/`tbl_sendgrid_event_logs` empty; SES keeps no recipient record (no config set). **The 50 bounced addresses are unrecoverable from the DBs** — only in SES bounce-feedback forwarded to the sender mailbox. Real remediation = add an SES **configuration set + bounce event destination** (CloudWatch/SNS/S3) so sends+bounces are logged and bad addresses auto-suppress. BOUNCE suppression list IS already enabled (repeat bouncers auto-drop). 8.4%/day is in AWS's warning band (>5%; suspension >10%).

## Crontab read (2026-06-16, SSM read-only on i-0392d1fca678f1a21)
Scheduler crontab is on the EC2 box (UTC). NO line fires at ~07:54 UTC ⇒ the daily batch is a FREQUENT cron (every-min/30-min/hourly) with an INTERNAL time/data gate (like syncPropertyMe/Tree's agency-local-05:00 gate) — why schedule/static analysis can't pin it; SQS capture is the definitive answer. `#SWAP-PAUSE`-commented (OFF): test-alarm-cron, test-alarm-notify-cron, mqtt/start-cron (disconnect). Email-capable crons: invitation-reminder + invitation-reminder-to-providerPays (*/30), invite-reminder (hourly), sync-property-me/tree (every-min import pipeline), insurance_renewal_invite (daily 22:00 UTC), check-expired-lease (0 */5). Crons are plain `curl GET` w/ a hardcoded **plaintext Basic-auth header** (security gap → docs/gaps.md item 5). The 2 open items (cron credential, 1,289 stale INVITED invitations) logged in docs/gaps.md (items 5 & 8).

Access used: `scripts/load-prod-env.sh` (sensor-prod secret → `sensor-mongo` Atlas helper + `sensor-mysql` over `db-tunnel-start.sh mysql`); DB is UTC.
