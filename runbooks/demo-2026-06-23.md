# Demo runbook — 2026-06-23 (install → simulate → remote test → audit report)

Prepared, not executed. Fill in `PROP=<id>` and `SERIAL=<hub serial>` when known.
All steps below are **read-only** except the clearly marked **[MUTATION — needs "go"]** ones.

## Pre-flight status (verified 2026-06-23, ~1h before demo)
- **Live API nodes:** `i-09e470062a5ff6ba1`, `i-0690fb2a2654ab2ad` (ASG — re-resolve if churned, see below).
- **Audit export healthy:** Chrome 128 `LAUNCH_OK` on both nodes; `auditExprotProgress=0` for 59117/59118/59120/59279/59325 (no stuck lock). See [[audit-export-puppeteer-chrome-missing]].
- **MQTT tooling:** `scripts/diag/mqtt-diag.py --mode {preflight|listen|simulate}`.
- **Pre-test comm path:** cron box `i-0392d1fca678f1a21` pub IP `54.153.177.72` = cron-whitelist id2 ACTIVE; auto-cron PAUSED (`#SWAP-PAUSE`) → manual trigger only. See [[sensor-pretest-comm-manual-trigger]].
- **SMS allowlist:** `tbl_sms_allowed_list` id3 = +61 401586481 ACTIVE (Kristyn). Agency acct 59120 phone corrected to 401586481 today (was typo 401596481).
- **Tenant recipient (demo props 23854/23855):** active-lease tenant 59280 `kristyn.heywood@saferhomesau.com.au`, phone +61 401586481.

### Re-resolve live API node if SSM says InvalidInstanceId
```
AWS_PROFILE=sensorsyn-mfa aws ssm describe-instance-information --region ap-southeast-2 \
  --query "InstanceInformationList[].InstanceId" --output text
# then probe which has the app env:
#   AWS_PROFILE=sensorsyn-mfa aws ssm send-command ... 'test -f /home/ec2-user/smoke_api/.env && echo YES'
```
Set `export SENSOR_API_INSTANCE_ID=i-09e470062a5ff6ba1` for all sql/mongo dispatch.

---

## Phase 0 — Property intake (when property ID given)
```
export PROP=<id>
cd /home/ubuntu/dev/sensorsyn
export SENSOR_API_INSTANCE_ID=i-09e470062a5ff6ba1
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/sql-read.py \
 "SELECT id,title,address,status,agency,agent,landlord,alarmTestDate,timezone FROM tbl_properties WHERE id=$PROP"
# active-lease tenant (the comm recipient):
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/sql-read.py \
 "SELECT p.id,p.status,lt.tenantId,a.name,a.email,a.phoneCode,a.phone FROM tbl_properties p \
  JOIN tbl_lease l ON l.propertyId=p.id AND l.status='1' \
  JOIN tbl_lease_tenants lt ON lt.leaseId=l.id AND lt.userType='primary' \
  JOIN tbl_admins a ON a.id=lt.tenantId WHERE p.id=$PROP"
# install job:
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/sql-read.py \
 "SELECT id,status,jobType,tradePersonId,jobTime FROM tbl_jobs WHERE propertyId=$PROP"
```
**Gate checklist:** tenant email present, phone == 401586481 (whitelisted), property will flip ACTIVE on install.
Status enum: 8=pre-install, 1=ACTIVE. Job status: 1 PENDING, 2 ACCEPTED, 5 COMPLETED.

---

## Phase 1 — Derive hub serial + monitor MQTT + installation progress
**Hub serial + devices (after pairing starts):**
```
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/sql-read.py \
 "SELECT serialNumber,controller,controllerId,alarmType,location,status,connectedStatus,jobId \
  FROM tbl_alarms WHERE propertyId=$PROP ORDER BY id"
```
The **controller/hub** row's `serialNumber` → `export SERIAL=<hub serial>`. Children point to it via `controllerId`.

**Monitor MQTT (inbound ground truth):**
```
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/mqtt-diag.py --mode preflight       # broker reachability
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/mqtt-diag.py --mode listen --seconds 60   # repeat as needed
```
Backup view: CloudWatch Logs Insights filter `payload->#` on the SmokeAPI pm2 out log = inbound device frames. See [[portal-test-mechanics-and-post-swap-failures]].

**Install progress:** job moves PENDING(1)→ACCEPTED(2)→COMPLETED(5); `tbl_alarms` rows appear; cross-system trace via `correlationId` ([[traceability-rule-saferops]]). **Tenant alarm comms only fire once job=COMPLETED(5) AND property=ACTIVE(1).**

---

## Phase 2 — Simulated events (physical triggers) → expected comms
| Action | MQTT expected | Comm → recipient | Gate |
|---|---|---|---|
| Smoke (can) | `CMD=ALERT ALARMTYPE=1 STATE=1` | tenant **alert sms+email** (Kristyn) | job COMPLETED + ACTIVE |
| Water leak | `CMD=ALERT` water STATE=1 | tenant **alert sms+email** | same |
| Tamper (open) | tamper STATE=1 | tenant **tampered sms+email** immediately | same |
| Tamper >15 min | scheduled check | **agent sms+email** (Kristyn 59120) — now deliverable after phone fix | same |
| Power loss (unplug hub) | `CMD=POWER POWERSTATE=2` | **agency email** "Hub switched to backup battery"; **+24h** → S127 escalation to Haven (NB: code=24h, agenda says 12h) | job COMPLETED + ACTIVE |

**Verify each event landed:**
```
# per-device MQTT activate/recover stream:
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/prod-read.py --collection tbl_alarm_logs --property $PROP --limit 20
# comms actually sent (email/SMS rows):
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/prod-read.py --collection tbl_logs --property $PROP --limit 20
```
(Confirm exact prod-read.py flags with `--help`; property filter is Number propertyId.)

---

## Phase 3 — Remote test + warn (pre-test) notification
**Remote test (portal-driven):** VERIFY/TEST to hub, QoS0, **6-min timeout**. ⚠️ **Wait >5 min after the hub has booted/been powered** before running the remote test — earlier tests return spurious "Hub did not respond". See [[portal-test-mechanics-and-post-swap-failures]].

**Warn notification = the 24h pre-test comm (S035 email + S056 SMS).** Property must be ACTIVE (it will be post-install). **[MUTATION — needs "go"]**
1. Save current `alarmTestDate` for revert.
2. Set `tbl_properties.alarmTestDate = now+24h` (UTC), set + trigger in the **same UTC half-hour bucket**.
3. On the cron box, run its existing notify-cron crontab line (Basic-auth token stays on box):
   ```
   # executed ON i-0392d1fca678f1a21 via SSM:
   crontab -l | grep 'test-alarm-notify-cron' | sed 's/^#SWAP-PAUSE //' | bash
   ```
4. Verify 2 rows in Mongo `tbl_logs`: email "Sensor Global - Scheduled Notification of Smoke Alarm Test" + SMS "Alarm Test".
5. Revert `alarmTestDate` to the saved value.

(I will prepare the parameterized set/revert write-script once the property is known; mirrors `sensor-admin-flag-reset.js` — FOR UPDATE snapshot, exact-row guard, before/after.)

---

## Phase 4 — Audit report
Admin clicks **Export Pdf** on the property's Audit History tab → `createPdfAuditReport` → S132 email with PDF to the requesting admin. Pre-verified healthy today (Chrome + flag=0).
**Pre-check before clicking, for whichever admin exports:**
```
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/sql-read.py \
 "SELECT id,email,auditExprotProgress FROM tbl_admins WHERE id=<exporting admin id>"   # must be 0
# audit rows that will populate the PDF:
AWS_PROFILE=sensorsyn-mfa python3 scripts/diag/prod-read.py --collection tbl_audit_histories --property $PROP --limit 5
```
If export silently fails: check `auditExprotProgress` didn't stick at 1 (reset via `sensor-admin-flag-reset.js` pattern) and re-confirm Chrome launch on the live node. Audit history reads Atlas `tbl_audit_histories` (PLURAL); records ALERT/TAMPERED/DISCONNECT/RECONNECT/BATTERY/HUSH/POWER (excludes TEST/RESET). See [[sensor-audit-history-mongo]].
